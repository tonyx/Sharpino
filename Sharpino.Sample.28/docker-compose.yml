version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: sharpino
      POSTGRES_USER: sharpino
      POSTGRES_PASSWORD: password
    ports:
      - "5435:5432"
    volumes:
      - ./db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql